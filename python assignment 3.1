# Coursera
#Python Assignment 3.1 
hrs = input("Enter Hours:")
rate=input("Enter Rates:")
try:
    h=float(hrs)
    r=float(rate)
except:
    quit()
if h > 40 :
    reg=r*h
    otp=(h-40.0)*(r*0.5)
    xp=reg+otp
else:
    xp=h*r
print(xp)
