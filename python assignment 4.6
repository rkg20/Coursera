# Coursera
#Python Assignment 
def computepay(h,r):
    ot = 0    
    if(h > 40):
        ot = h - 40
    p = (h-ot)*r + ot*r*1.5
    return p

hrs = float(raw_input("Enter Hours:"))
rate = float(raw_input("Enter Rate/hrs:"))
pay = computepay(hrs, rate)
print("Pay",pay)
